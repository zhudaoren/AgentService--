# P1 阶段代码修改文档

> **项目**: AgentService Platform Phase1  
> **文档日期**: 2026-08-01  
> **Git Commits**: dcbe0cd → 4cbdda0 (共 8 次提交)

---

## 目录

1. [基础设施修复](#1-基础设施修复)
2. [API 网关修复](#2-api-网关修复)
3. [LLM 适配器增强](#3-llm-适配器增强)
4. [Agent 服务修复与功能新增](#4-agent-服务修复与功能新增)
5. [前端修改](#5-前端修改)
6. [修改文件清单](#6-修改文件清单)
7. [Git 提交记录](#7-git-提交记录)

---

## 1. 基础设施修复

### 1.1 数据库连接 - 环境变量名不匹配

**文件**: `services/shared/infrastructure/db/__init__.py`  
**Commit**: dcbe0cd

**问题**: `.env` 文件中使用 `MYSQL_ROOT_PASSWORD`，但代码 `config/__init__.py` 读取的是 `MYSQL_PASSWORD`，导致数据库连接报 `Access denied`。

**修复**: 修正 `.env` 中环境变量名为 `MYSQL_PASSWORD`。

### 1.2 数据库连接 - SQLAlchemy 异步 SQL 执行报错

**文件**: `services/shared/infrastructure/db/__init__.py` (L50)  
**Commit**: dcbe0cd

**问题**: `await conn.execute("SELECT 1")` 直接传字符串，SQLAlchemy 2.x 异步模式要求使用 `text()` 包装裸 SQL。

**修复**:
```python
# 修改前
await conn.execute("SELECT 1")

# 修改后
from sqlalchemy import text
await conn.execute(text("SELECT 1"))
```

---

## 2. API 网关修复

### 2.1 SSE 流式代理连接提前关闭

**文件**: `services/gateway/main.py` (L19-L40, L129-L145)  
**Commit**: dcbe0cd

**问题**: Gateway 使用临时 `httpx.AsyncClient` 转发请求，SSE 流式响应在 `httpx` 连接池回收时被提前关闭，报 `httpx.StreamClosed: Attempted to read or stream content, but the stream has been closed`。

**修复**:
- 使用全局共享的 `httpx.AsyncClient`（通过 `lifespan` 管理生命周期），配置连接池 `max_connections=50, max_keepalive_connections=20`
- 新增 `_proxy_stream()` 函数，通过 `client.build_request()` + `client.send(stream=True)` 正确管理流式连接
- 在 `stream_gen()` 生成器的 `finally` 块中调用 `response.aclose()` 确保资源释放

```python
# 全局客户端
_httpx_client: httpx.AsyncClient | None = None

@asynccontextmanager
async def lifespan(app: FastAPI):
    global _httpx_client
    _httpx_client = httpx.AsyncClient(
        timeout=300,
        limits=httpx.Limits(max_connections=50, max_keepalive_connections=20),
    )
    yield
    if _httpx_client:
        await _httpx_client.aclose()

# 流式代理
async def _proxy_stream(client, method, url, body, headers):
    request = client.build_request(method, url, content=body, headers=headers)
    response = await client.send(request, stream=True)
    async def stream_gen():
        try:
            async for chunk in response.aiter_bytes():
                yield chunk
        finally:
            await response.aclose()
    return StreamingResponse(stream_gen(), status_code=response.status_code, headers=dict(response.headers))
```

### 2.2 路由尾部斜杠导致 307 重定向

**文件**: `services/agent-svc/routers/agent.py` (L37, L47), `services/agent-svc/routers/llm_config.py` (L22, L32)  
**Commit**: c91424e

**问题**: 后端路由注册为 `@router.post("/")`，完整路径为 `/api/v1/agents/`（带尾部斜杠）。前端请求 `/api/v1/agents`（不带斜杠），FastAPI 返回 307 重定向。Gateway 的 httpx 代理转发 POST 请求时，307 不会保留请求体，导致创建 Agent 失败。

**修复**: 将根路由的 `"/"` 改为 `""`：
```python
# 修改前
@agent_router.post("/", response_model=ApiResponse)
@agent_router.get("/", response_model=ApiResponse)

# 修改后
@agent_router.post("", response_model=ApiResponse)
@agent_router.get("", response_model=ApiResponse)
```

---

## 3. LLM 适配器增强

### 3.1 新增多 LLM 提供商支持

**文件**: `services/shared/domain/llm_adapter.py` (L20, L89)  
**Commit**: dcbe0cd

**问题**: 原代码不支持 `moonshot`、`kimi`、`zhipu`、`glm`、`siliconflow` 等提供商。

**修复**:
- `SUPPORTED_PROVIDERS` 列表新增 5 个提供商
- 将 `moonshot`/`kimi`/`zhipu`/`siliconflow` 归入 `OPENAI_COMPATIBLE_PROVIDERS`，复用 `ChatOpenAI` 适配
- `glm` 使用 `ChatZhipuAI` 适配

### 3.2 提供商参数白名单 - 按需传参

**文件**: `services/shared/domain/llm_adapter.py` (L24-L35, L64-L80)  
**Commit**: dcbe0cd

**问题**: 部分大模型 API 不支持 `temperature`、`max_tokens`、`top_p` 等参数，统一传参会导致调用报错。

**修复**:
- 新增 `PROVIDER_SUPPORTED_PARAMS` 字典，定义每个提供商支持的参数白名单
- 新增 `_get_model_kwargs()` 方法，只构建当前 provider 支持的参数
- 新增 `get_provider_params_info()` 静态方法，供前端查询各 provider 支持的参数

```python
PROVIDER_SUPPORTED_PARAMS = {
    "openai": ["temperature", "max_tokens", "top_p"],
    "ollama": ["temperature", "top_p", "num_predict"],
    # ...
}
```

### 3.3 模型参数异常兜底降级机制

**文件**: `services/shared/domain/llm_adapter.py` (L57, L82-L172, L186-L230)  
**Commit**: dcbe0cd

**问题**: 即使按白名单过滤参数，某些模型在实际调用时仍可能因参数值不兼容而报错（如某些模型只接受 `temperature=1`）。

**修复**: 实现两阶段降级机制：

**阶段1 - 实例创建阶段**:
- `_create_llm()` 先尝试带参数创建 LLM 实例
- 若失败，调用 `_create_llm_by_provider(with_params=False)` 创建无参数实例
- 标记 `self._fallback_used = True`

**阶段2 - 调用阶段**:
- `invoke()` 和 `stream()` 方法捕获异常后，若尚未降级则调用 `_reset_llm_with_fallback()` 重置实例为默认参数版本并重试一次
- 通过 `is_fallback_used` 属性向外暴露降级状态

### 3.4 后端降级提示传递

**文件**: `services/agent-svc/services/llm_service.py` (L176-L180), `services/chat-svc/services/chat_service.py` (L310-L314)  
**Commit**: dcbe0cd

**修复**: 在 LLM 连通性测试和对话 SSE 结束事件中，附加 `fallback` 字段和 `fallback_message`，供前端展示降级提示。

```python
# llm_service.py - 测试连通性
if adapter.is_fallback_used:
    result["fallback"] = True
    result["message"] = "连接成功（已自动降级为模型默认参数...）"

# chat_service.py - SSE done 事件
if adapter.is_fallback_used:
    done_payload["fallback"] = True
    done_payload["fallback_message"] = "已自动降级为模型默认参数..."
```

### 3.5 新增提供商参数查询 API

**文件**: `services/agent-svc/routers/llm_config.py` (L76-L88)  
**Commit**: dcbe0cd

**新增接口**:
```
GET /api/v1/llm-configs/providers/params
```

返回各提供商支持的参数列表，供前端动态展示配置项。

---

## 4. Agent 服务修复与功能新增

### 4.1 修复 SQLAlchemy 异步懒加载 MissingGreenlet 错误

**文件**: `services/agent-svc/services/agent_service.py` (L146-L167, L260-L300, L310-L335)  
**Commits**: caf65e8, 8ff26a3, 7c05223

**问题**: `create_agent`、`clone_agent`、`update_agent`、`change_status` 方法在 `db.flush()` 后直接调用 `_to_out(agent)`，访问 `agent.llm_config` 懒加载关系时触发 `MissingGreenlet: greenlet_spawn has not been called` 错误。

**修复**: 统一采用系统化方案 —— **所有写操作在 flush 后用 `_get_by_id` 重新查询**，该方法已使用 `selectinload(Agent.llm_config)` 预加载关系：

```python
async def create_agent(self, db, payload):
    # ... 创建 agent ...
    await db.flush()
    # 重新查询以确保关系正确加载
    agent = await self._get_by_id(db, agent_id)
    return await self._to_out(agent)
```

涉及 4 个方法：`create_agent`、`clone_agent`、`update_agent`、`change_status`。

### 4.2 新增提示词工程专家官方 Agent

**文件**: `services/agent-svc/services/agent_service.py` (L100-L119, L459-L471)  
**Commit**: b4f6572

**新增内容**:
- 在 `OFFICIAL_AGENTS` 列表中新增第 6 个官方 Agent「提示词工程专家」
- `config` 中标记 `is_prompt_polisher: True` 用于后续识别
- 系统提示词定义了 6 条润色原则：角色定义清晰、行为约束精确、输出格式规范、边界条件明确、语言简洁有力、保留用户意图
- 初始化时优先匹配 `deepseek` 模型的 LLM 配置

### 4.3 新增 AI 润色系统提示词 API

**文件**: `services/agent-svc/routers/agent.py` (L24-L25, L117-L126), `services/agent-svc/services/agent_service.py` (L340-L397)  
**Commit**: b4f6572

**新增接口**:
```
POST /api/v1/agents/polish-prompt
Body: { "raw_prompt": "用户原始提示词" }
Response: { "polished_prompt": "润色后的提示词", "fallback": false }
```

**实现逻辑**:
- `polish_system_prompt()` 方法查找 `config.is_prompt_polisher == True` 的官方 Agent
- 使用该 Agent 的 LLM 配置（低温度 0.3 保证输出稳定）调用大模型
- 返回润色后的提示词和降级标记

### 4.4 官方 Agent 初始化改为增量更新模式

**文件**: `services/agent-svc/services/agent_service.py` (L412-L508)  
**Commit**: bcbb186

**问题**: 原逻辑为「全有或全无」—— 检测到已有官方 Agent 就跳过整个初始化，导致新增的提示词工程专家 Agent 无法被创建。

**修复**: 改为增量更新模式，按名称匹配：
- 已存在的 Agent 跳过
- 不存在的 Agent 创建
- 日志输出 `新增 N 个, 跳过 M 个`

```python
# 查询已存在的官方 Agent 名称集合
existing_names = set(existing_result.scalars().all())

for agent_def in OFFICIAL_AGENTS:
    if agent_def["name"] in existing_names:
        skipped_count += 1
        continue
    # ... 创建新 Agent ...
    created_count += 1
```

---

## 5. 前端修改

### 5.1 前端 page_size 超限导致 422 错误

**文件**: 多个前端视图文件  
**Commit**: dcbe0cd

**问题**: 前端请求 `page_size=200`，后端限制 `le=100`，返回 422 Unprocessable Entity。

**修复**: 将前端各页面的 `page_size` 从 200 调整为 100，与后端 `Query(20, ge=1, le=100)` 限制对齐。

### 5.2 LLM 配置页 - 动态参数展示

**文件**: `frontend/src/views/llm-config/index.vue` (L144-L145, L198-L209, L294-L304, L369-L378)  
**Commit**: dcbe0cd

**修复**:
- 页面加载时调用 `fetchProviderParams()` 获取各 provider 支持的参数
- 通过 `currentSupportedParams` 计算属性动态显示/隐藏 `temperature`、`max_tokens`、`top_p` 配置项
- 切换 provider 时自动重置默认参数
- 提交时只发送支持的参数（`cleanedParams`）

### 5.3 LLM 配置页 - API Key 编辑回填

**文件**: `frontend/src/views/llm-config/index.vue` (L279-L290, L317-L323)  
**Commit**: dcbe0cd

**问题**: 编辑 LLM 配置时 API Key 输入框为空，用户体验不佳。

**修复**:
- 编辑时回填后端返回的掩码值（如 `sk-***xxxx`），`a-input-password` 组件自动显示为黑点密文
- 提交时检测 `api_key` 是否仍为掩码值（未修改），若是则不传该字段，后端保留原密钥

### 5.4 LLM 配置测试 - 降级提示

**文件**: `frontend/src/views/llm-config/index.vue`  
**Commit**: dcbe0cd

**修复**: 测试连通性返回 `fallback=true` 时，显示黄色警告「连接成功（已自动降级为模型默认参数...）」。

### 5.5 对话页 - 降级提示

**文件**: `frontend/src/views/chat/index.vue` (L450-L453)  
**Commit**: dcbe0cd

**修复**: SSE `done` 事件携带 `fallback` 字段时，弹出黄色提示告知用户已降级。

### 5.6 前端 API 新增方法

**文件**: `frontend/src/api/index.js` (L60, L73)  
**Commit**: dcbe0cd, b4f6572

新增两个 API 方法：
```javascript
// LLM 配置 - 获取提供商支持的参数
getProviderParams: () => request.get('/llm-configs/providers/params'),

// Agent - AI 润色系统提示词
polishPrompt: (raw_prompt) => request.post('/agents/polish-prompt', { raw_prompt }),
```

### 5.7 Agent 管理页 - AI 润色对比弹窗

**文件**: `frontend/src/views/agent/index.vue` (L142-L161, L229-L287, L528-L582)  
**Commit**: b4f6572, 4cbdda0

**功能**: 在 Agent 创建/编辑表单的系统提示词输入框右下角添加「⚡ AI 润色」按钮，点击后弹出对比弹窗：

- **左侧（只读）**: 显示原始提示词
- **右侧（可编辑）**: 显示润色后的提示词，用户可直接编辑
- **再次 AI 润色**: 对右侧当前内容再次调用润色接口，支持反复迭代
- **确认使用**: 将右侧最终版提示词填回表单
- **取消**: 放弃修改，原表单不受影响

**状态变量**:
```javascript
const polishVisible = ref(false)
const polishOriginal = ref('')
const polishResult = ref('')
const polishing = ref(false)
```

---

## 6. 修改文件清单

| # | 文件路径 | 修改类型 | 说明 |
|---|---------|---------|------|
| 1 | `services/shared/infrastructure/db/__init__.py` | 修复 | SQLAlchemy text() 包装、连接池配置 |
| 2 | `services/gateway/main.py` | 修复 | 全局 httpx 客户端、SSE 流式代理 |
| 3 | `services/shared/domain/llm_adapter.py` | 增强 | 多提供商支持、参数白名单、降级兜底 |
| 4 | `services/agent-svc/routers/agent.py` | 修复+新增 | 路由斜杠修复、polish-prompt 接口 |
| 5 | `services/agent-svc/routers/llm_config.py` | 修复+新增 | 路由斜杠修复、providers/params 接口 |
| 6 | `services/agent-svc/services/agent_service.py` | 修复+新增 | 懒加载修复、提示词专家 Agent、增量初始化、润色方法 |
| 7 | `services/agent-svc/services/llm_service.py` | 增强 | 测试连通性返回降级标记 |
| 8 | `services/chat-svc/services/chat_service.py` | 增强 | SSE done 事件携带降级提示 |
| 9 | `frontend/src/api/index.js` | 新增 | getProviderParams、polishPrompt 方法 |
| 10 | `frontend/src/views/llm-config/index.vue` | 增强 | 动态参数展示、API Key 回填、降级提示 |
| 11 | `frontend/src/views/agent/index.vue` | 新增 | AI 润色按钮、对比弹窗 |
| 12 | `frontend/src/views/chat/index.vue` | 增强 | 降级提示 |
| 13 | `.env` | 修复 | MYSQL_PASSWORD 环境变量名 |

---

## 7. Git 提交记录

| Commit | 类型 | 说明 |
|--------|------|------|
| `dcbe0cd` | init | 项目初始化（含基础设施修复、网关修复、LLM 适配器增强、前端动态参数） |
| `c91424e` | fix | 修复路由尾部斜杠导致的 307 重定向问题 |
| `caf65e8` | fix | 修复 create_agent 中懒加载 llm_config 导致的 MissingGreenlet 错误 |
| `8ff26a3` | fix | 修复 clone_agent 中懒加载 llm_config 导致的 MissingGreenlet 错误 |
| `7c05223` | refactor | 系统化修复懒加载问题 - flush 后统一用 _get_by_id 重新查询 |
| `b4f6572` | feat | 新增提示词工程专家 Agent 及 AI 润色系统提示词功能 |
| `bcbb186` | fix | 官方 Agent 初始化改为增量更新模式 |
| `4cbdda0` | feat | AI 润色系统提示词增加对比弹窗 |
