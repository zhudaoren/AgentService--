# AgentService 平台 P2 阶段软件测试文档

> 版本: v1.0 ｜ 适用范围: P2 阶段新增功能 ｜ 编写日期: 2026-08-05

***

## 1. 测试概述

### 1.1 测试目标

验证 P2 阶段相对 P1 新增功能的正确性、稳定性与边界容错能力，确保各微服务（tool-svc / chat-svc / mem-svc / agent-svc）以及前端模块在 MCP 工具集成、Skill 渐进式披露、ReAct 对话编排、记忆深化四个维度功能可用。

### 1.2 P2 新增功能范围

| 模块         | 服务        | 核心能力                                         |
| ---------- | --------- | -------------------------------------------- |
| MCP 服务管理   | tool-svc  | SSE/STDIO 双模接入、连接管理、工具发现、工具调用日志              |
| Skill 管理   | tool-svc  | CRUD、3 级渐进式披露、本地/在线导入、Token 估算               |
| Agent 工具绑定 | agent-svc | Agent-MCP / Agent-Skill 绑定、tools-summary 汇总  |
| ReAct 对话编排 | chat-svc  | 流式 SSE、Function Calling + 文本 ReAct 兜底、停止/重生成 |
| 记忆深化       | mem-svc   | Redis 短期缓存（内存回退）、长期记忆版本化、记忆摘要                |

### 1.3 测试环境

- **后端**: Python 3.11 + FastAPI + SQLAlchemy(async) + MySQL 8.0 + Redis 7
- **前端**: Vue3 + Vite + Axios + Ant Design Vue
- **依赖服务**: MySQL（`infra/mysql/init.sql` 初始化）、Redis（可选）、LLM API（OpenAI/DeepSeek 兼容）
- **服务端口**: gateway=8000, agent-svc=8001, chat-svc=8002, tool-svc=8003, mem-svc=8004

***

## 2. 测试策略

### 2.1 测试分层

| 层级   | 范围                                | 工具建议                       |
| ---- | --------------------------------- | -------------------------- |
| 单元测试 | services/ 内业务方法、shared/domain 适配器 | pytest + pytest-asyncio    |
| 接口测试 | routers/ 下 REST API               | pytest + httpx.AsyncClient |
| 集成测试 | 跨服务调用（chat→tool→agent）            | pytest + httpx + 真实 DB     |
| 前端测试 | Vue 页面交互                          | Vitest + @vue/test-utils   |
| 手工验收 | 端到端 ReAct 流式对话                    | 浏览器 + curl SSE             |

### 2.2 通用前置条件

1. 已执行 `infra/mysql/init.sql`，20 张表创建完成
2. 已通过 P1 流程创建至少 1 个 LLM 配置和 1 个 Agent
3. Redis 可用（用于短期记忆缓存测试）；如不可用需验证内存回退
4. 准备一个可用的 MCP 服务（SSE URL 或 STDIO 命令）用于工具发现/调用测试

***

## 3. 功能测试用例

### 3.1 MCP 服务管理（tool-svc）

#### 3.1.1 MCP CRUD

| 用例 ID    | 用例名称               | 前置条件        | 操作步骤                                                               | 预期结果                                                |
| -------- | ------------------ | ----------- | ------------------------------------------------------------------ | --------------------------------------------------- |
| MCP-C-01 | 创建 SSE 模式服务        | 无           | POST `/api/v1/mcp-services`，body 含 `name/mode=sse/sse_url`         | 返回 200，status=disconnected，DB `mcp_services` 新增 1 条 |
| MCP-C-02 | 创建 STDIO 模式服务      | 无           | POST，mode=stdio，`stdio_config.command` 必填                          | 创建成功，sse\_url=null                                  |
| MCP-C-03 | 创建 SSE 缺 url       | 无           | POST，mode=sse，sse\_url 缺失                                          | 返回 400，提示 "sse\_url 必填"                             |
| MCP-C-04 | 创建 STDIO 缺 command | 无           | POST，mode=stdio，无 command                                          | 返回 400，提示 "command 必填"                              |
| MCP-C-05 | 创建非法 mode          | 无           | mode=websocket                                                     | 返回 400，提示 "mode 必须是 sse 或 stdio"                    |
| MCP-C-06 | 列表分页与筛选            | 已有多条数据      | GET `/api/v1/mcp-services?page=1&page_size=10&mode=sse&keyword=xx` | 返回分页结构 `{items, total, page, page_size}`            |
| MCP-C-07 | 更新服务               | 存在 mcp\_id  | PUT `/api/v1/mcp-services/{id}`，修改 name                            | 返回 200，DB 字段更新                                      |
| MCP-C-08 | 删除服务（未连接）          | 存在 mcp\_id  | DELETE                                                             | 返回 200，DB 软硬删除，关联 `mcp_tools` 级联删除                  |
| MCP-C-09 | 删除服务（已连接）          | 服务已 connect | DELETE                                                             | 先 disconnect 释放 adapter，再删除                         |
| MCP-C-10 | 查询不存在服务            | 无           | GET `/api/v1/mcp-services/{不存在的id}`                                | 返回 404，message 含 "MCP服务不存在"                         |

#### 3.1.2 连接管理

| 用例 ID    | 用例名称          | 前置条件        | 操作步骤                                     | 预期结果                                                               |
| -------- | ------------- | ----------- | ---------------------------------------- | ------------------------------------------------------------------ |
| MCP-S-01 | SSE 连接成功      | SSE URL 可达  | POST `/api/v1/mcp-services/{id}/connect` | 返回 `{status:connected}`，DB status=connected，`last_connected_at` 更新 |
| MCP-S-02 | STDIO 进程启动    | command 有效  | POST connect                             | 进程拉起，status=connected                                              |
| MCP-S-03 | 连接失败（URL 不可达） | URL 错误      | POST connect                             | 返回 400，DB status=error，`error_message` 记录原因                        |
| MCP-S-04 | STDIO 启动失败    | command 不存在 | POST connect                             | status=error，错误信息含 "STDIO进程启动失败"                                   |
| MCP-S-05 | 断开连接          | 已连接         | POST `/disconnect`                       | status=disconnected，adapter 从内存字典移除，STDIO 进程被 terminate            |
| MCP-S-06 | 重复连接          | 已连接         | 再次 POST connect                          | 不重复创建 adapter（使用 `_get_or_create_adapter` 缓存）                      |

#### 3.1.3 工具发现与管理

| 用例 ID    | 用例名称         | 前置条件      | 操作步骤                                                    | 预期结果                                                       |
| -------- | ------------ | --------- | ------------------------------------------------------- | ---------------------------------------------------------- |
| MCP-T-01 | 发现工具（SSE）    | SSE 已连接   | POST `/discover`                                        | 调用 `adapter.list_tools()`，工具批量 upsert 到 `mcp_tools`，返回工具列表 |
| MCP-T-02 | 发现工具（STDIO）  | STDIO 已连接 | POST `/discover`                                        | 通过 JSON-RPC `tools/list` 获取，正常 upsert                      |
| MCP-T-03 | 发现工具自动连接     | 服务未连接     | POST `/discover`                                        | 自动 connect 后再 list\_tools                                  |
| MCP-T-04 | 工具列表查询       | 已发现工具     | GET `/api/v1/mcp-services/{id}/tools?enabled=true`      | 返回工具列表，enabled 过滤生效                                        |
| MCP-T-05 | 启用/禁用工具      | 存在工具      | POST `/tools/{tool_name}/toggle` body `{enabled:false}` | DB `mcp_tools.enabled` 更新                                  |
| MCP-T-06 | 切换不存在工具      | 无         | POST toggle 不存在的 tool\_name                             | 返回 404 "工具不存在"                                             |
| MCP-T-07 | 重复发现（upsert） | 已有工具      | 再次 POST `/discover`                                     | 走 `on_duplicate_key_update`，不重复插入，仅更新 description/schema   |

#### 3.1.4 工具调用与日志

| 用例 ID       | 用例名称           | 前置条件               | 操作步骤                                                             | 预期结果                                                                            |
| ----------- | -------------- | ------------------ | ---------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| MCP-CALL-01 | 通过 ID 调用工具     | MCP 已连接且工具 enabled | POST `/api/v1/tools/call`，含 `mcp_service_id/tool_name/arguments` | 返回 `{tool_name, status:success, result, duration_ms}`，`mcp_tools.usage_count+1` |
| MCP-CALL-02 | 通过 name 调用     | 知道服务名              | 用 `mcp_service_name` 替代 id                                       | 同上                                                                              |
| MCP-CALL-03 | 既无 id 也无 name  | 无                  | body 缺少两者                                                        | 返回 400 "必须提供 mcp\_service\_id 或 mcp\_service\_name"                             |
| MCP-CALL-04 | 调用已禁用工具        | 工具 enabled=false   | POST call                                                        | 返回 400 "工具已禁用"                                                                  |
| MCP-CALL-05 | 自动连接后调用        | 服务未连接              | POST call                                                        | 自动 connect，成功后调用                                                                |
| MCP-CALL-06 | 调用超时           | timeout=1，工具慢      | POST call                                                        | 抛 MCPException，日志记录 status=failed                                               |
| MCP-CALL-07 | 调用失败日志写入       | 触发异常               | POST call 失败                                                     | `tool_call_logs` 表新增 1 条 status=failed，`error_message` 非空                       |
| MCP-CALL-08 | 调用日志查询         | 已有日志               | GET `/api/v1/tool-call-logs?agent_id=xx&status=failed`           | 分页返回，多条件过滤生效                                                                    |
| MCP-CALL-09 | 调用日志含 duration | 已有调用               | GET logs                                                         | `duration_ms` 字段 > 0                                                            |

***

### 3.2 Skill 管理（tool-svc）

#### 3.2.1 Skill CRUD

| 用例 ID   | 用例名称              | 前置条件         | 操作步骤                                                              | 预期结果                                             |
| ------- | ----------------- | ------------ | ----------------------------------------------------------------- | ------------------------------------------------ |
| SK-C-01 | 创建 Skill 含 levels | 无            | POST `/api/v1/skills`，levels 含 0/1/2 三级                           | 返回 200，`skill_levels` 表新增 3 条，`token_count` 自动估算 |
| SK-C-02 | 创建 Skill 无 levels | 无            | POST 不传 levels                                                    | 创建成功，levels 为空数组                                 |
| SK-C-03 | 列表分页与筛选           | 已有多条         | GET `/api/v1/skills?category=programming&enabled=true&keyword=py` | 分页结构正确，过滤生效，每项含 levels                           |
| SK-C-04 | 详情查询              | 存在 skill\_id | GET `/api/v1/skills/{id}`                                         | 返回完整 Skill + levels 列表                           |
| SK-C-05 | 更新 Skill          | 存在           | PUT `/api/v1/skills/{id}`                                         | 字段更新成功                                           |
| SK-C-06 | 启用/禁用             | 存在           | POST `/api/v1/skills/{id}/toggle` `{enabled:false}`               | enabled 字段更新                                     |
| SK-C-07 | 删除 Skill          | 存在           | DELETE                                                            | 级联删除 `skill_levels`，`agent_skill_bindings`       |
| SK-C-08 | 查询不存在             | 无            | GET 不存在 id                                                        | 返回 404 "Skill不存在"                                |

#### 3.2.2 渐进式披露（核心）

| 用例 ID   | 用例名称         | 前置条件             | 操作步骤                                          | 预期结果                                                  |
| ------- | ------------ | ---------------- | --------------------------------------------- | ----------------------------------------------------- |
| SK-L-01 | 查询层级列表       | 存在 skill\_id     | GET `/api/v1/skills/{id}/levels`              | 按 level 升序返回，含 0/1/2 三级                               |
| SK-L-02 | Level 0 概要加载 | 存在               | GET `/api/v1/skills/{id}/progressive?level=0` | 返回 `prompt_text`，`actual_tokens` ≤ 300（预算）或日志记录超限     |
| SK-L-03 | Level 1 完整加载 | 存在               | GET progressive?level=1                       | `budget_tokens=3000`，prompt 包含 Level 0+1 内容           |
| SK-L-04 | Level 2 深度加载 | 存在               | GET progressive?level=2                       | `budget_tokens=10000`，prompt 包含 Level 0+1+2           |
| SK-L-05 | Token 估算准确性  | 已知文本             | 调用 `SkillManager.estimate_tokens`             | 中文 1 字≈1 token，英文 2 词≈1 token                         |
| SK-L-06 | 超预算截断        | Level 2 内容超长     | GET progressive?level=2                       | 走 `_truncate_to_token_budget`，末尾出现 "... (内容过长已按预算截断)" |
| SK-L-07 | Level 1 缺失场景 | Skill 仅含 level 0 | GET progressive?level=1                       | prompt 提示 "本技能未提供 Level 1 内容"                         |

#### 3.2.3 Skill 导入

| 用例 ID   | 用例名称                       | 前置条件      | 操作步骤                                                              | 预期结果                                                                                           |
| ------- | -------------------------- | --------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| SK-I-01 | 本地 Markdown 导入             | 准备 .md 文件 | POST `/api/v1/skills/import/local` (multipart)                    | 解析 frontmatter/name/description，自动生成 3 级 levels，`source=local`，文件落盘到 `data/skills_uploads/日期/` |
| SK-I-02 | 本地 JSON 导入                 | 准备 .json  | 同上                                                                | JSON 解析成功                                                                                      |
| SK-I-03 | 本地 .zip 导入                 | 准备 .zip   | 同上                                                                | 返回 400 ".zip 格式暂不支持"                                                                           |
| SK-I-04 | 在线 URL 导入                  | URL 可访问   | POST `/api/v1/skills/import/online` `{source_url, import_format}` | httpx 拉取内容，`source=online`，`source_url` 记录                                                     |
| SK-I-05 | 在线 URL 不可达                 | URL 404   | 同上                                                                | 返回 400 "拉取在线内容失败"                                                                              |
| SK-I-06 | Markdown 解析（无 frontmatter） | 纯正文 .md   | 导入                                                                | 从首个 `# 标题` 提取 name，首段非标题文本作 description                                                        |
| SK-I-07 | 三级内容自动构建                   | 导入成功      | 查询 levels                                                         | Level 0=概要索引，Level 1=完整说明（>8000 字截断），Level 2=深度+高级+边界                                          |

***

### 3.3 Agent 工具绑定（agent-svc）

| 用例 ID | 用例名称              | 前置条件                | 操作步骤                                          | 预期结果                                                                                           |
| ----- | ----------------- | ------------------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| AB-01 | 绑定 MCP 到 Agent    | Agent、MCP 存在        | POST `/api/v1/agents/{agent_id}/mcp-bindings` | `agent_mcp_bindings` 新增 1 条，重复绑定走 UNIQUE 约束                                                    |
| AB-02 | 解绑 MCP            | 已绑定                 | DELETE `/mcp-bindings/{mcp_service_id}`       | 绑定记录删除                                                                                         |
| AB-03 | 查询 Agent 的 MCP 绑定 | 已绑定多条               | GET `/api/v1/agents/{agent_id}/mcp-bindings`  | 返回绑定列表                                                                                         |
| AB-04 | 绑定 Skill          | Skill 存在            | POST `/skill-bindings` 含 priority             | `agent_skill_bindings` 新增，priority 生效                                                          |
| AB-05 | 解绑 Skill          | 已绑定                 | DELETE `/skill-bindings/{skill_id}`           | 删除成功                                                                                           |
| AB-06 | tools-summary 汇总  | Agent 已绑定 MCP+Skill | GET `/api/v1/agents/{agent_id}/tools-summary` | 返回 `{mcp_services:[{name,mode,status,tools[]}], skills:[{name,description,...}]}`，仅含 enabled 项 |
| AB-07 | tools-summary 无绑定 | Agent 无绑定           | GET                                           | 返回空数组，不报错                                                                                      |
| AB-08 | 绑定不存在 Agent       | 无                   | POST 到不存在 agent\_id                           | 返回 404                                                                                         |

***

### 3.4 ReAct 对话编排（chat-svc）

#### 3.4.1 流式对话基础

| 用例 ID   | 用例名称       | 前置条件                 | 操作步骤                                                       | 预期结果                                                             |
| ------- | ---------- | -------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------- |
| CHAT-01 | 纯文本对话（无工具） | Agent 无 MCP/Skill 绑定 | POST `/api/chat` `{conversation_id, content, stream:true}` | SSE 流式返回 `event: message` 多片 + `event: done`，done 含 `message_id` |
| CHAT-02 | 消息内容为空     | 无                    | content=""                                                 | 返回 400 "消息内容不能为空"                                                |
| CHAT-03 | 会话不存在      | 无                    | 不存在的 conversation\_id                                      | 返回 404 "会话不存在"                                                   |
| CHAT-04 | Agent 已停止  | agent.status=stopped | 创建会话                                                       | 返回 400 "Agent 已停止"                                               |
| CHAT-05 | LLM 配置缺失   | Agent 无 llm\_config  | 发起对话                                                       | 返回 404 "LLM 配置不存在"                                               |
| CHAT-06 | 用户消息持久化    | 对话完成                 | 查询 messages 表                                              | 新增 user + assistant 两条，`conversation.message_count` +2           |

#### 3.4.2 ReAct 工具调用

| 用例 ID    | 用例名称                           | 前置条件                                    | 操作步骤             | 预期结果                                                                                              |
| -------- | ------------------------------ | --------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------- |
| REACT-01 | Function Calling 模式            | Agent 绑定 MCP，provider=openai/deepseek 等 | POST chat，提问需调工具 | SSE 推送 `event: tool_call`（含 index/tool\_name/status/result），最终 `event: done` 含 `react_iterations` |
| REACT-02 | 文本 ReAct 兜底                    | provider 不支持 bind\_tools                | 同上               | LLM 输出 ACTION/ARGS 格式，`_parse_react_tool_calls_from_text` 解析成功                                    |
| REACT-03 | `<\|FunctionCallBegin\|>` 格式解析 | 同 REACT-02                              | LLM 输出该格式        | 正则匹配解析为工具调用                                                                                       |
| REACT-04 | 多轮工具调用                         | 任务需 2+ 轮工具                              | POST chat        | ReAct 循环 ≤ 8 次，每轮持久化 tool\_call + tool\_result 消息                                                 |
| REACT-05 | 达到最大迭代                         | 故意构造无限工具调用                              | POST chat        | 第 8 轮后注入 "请直接给出最终总结"，LLM 输出最终答案                                                                   |
| REACT-06 | 工具调用失败降级                       | 工具不可用                                   | POST chat        | tool\_call 事件 status=failed，LLM 收到 "工具调用失败" observation 后继续                                       |
| REACT-07 | tool-svc 不可用                   | httpx 未安装/服务下线                          | POST chat        | `_call_tool_via_http` 返回降级结构，不抛异常                                                                 |
| REACT-08 | tools-summary 不可用              | agent-svc 下线                            | POST chat        | `_fetch_tools_summary_http` 返回 None，`enable_react=false`，走纯文本对话                                   |
| REACT-09 | system\_prompt 注入              | Agent 绑定 MCP+Skill                      | 检查 LLM messages  | system\_prompt 含 Skills Level0 概要 + MCP 工具列表 + 长期记忆                                               |
| REACT-10 | tool\_call 消息持久化               | ReAct 完成                                | 查询 messages      | 新增 tool\_call、tool\_result 类型消息，`tool_calls`/`tool_results` JSON 字段非空                             |

#### 3.4.3 停止与重生成

| 用例 ID        | 用例名称             | 前置条件            | 操作步骤                                                         | 预期结果                                                        |
| ------------ | ---------------- | --------------- | ------------------------------------------------------------ | ----------------------------------------------------------- |
| CHAT-STOP-01 | 停止正在生成的流         | 流式生成中           | POST `/api/chat/stop` `{conversation_id}`                    | 返回 True，SLE 流中断，推送 `event: done` `{stopped:true}`           |
| CHAT-STOP-02 | 停止无运行流           | 无运行流            | POST stop                                                    | 返回 False                                                    |
| CHAT-REG-01  | 重新生成最后消息         | 存在 assistant 消息 | POST `/api/chat/regenerate` `{conversation_id, stream:true}` | 删除最后 assistant + 其后 tool\_call/tool\_result，基于上一条 user 重新生成 |
| CHAT-REG-02  | 无 assistant 可重生成 | 会话无 assistant   | POST regenerate                                              | 返回 404 "无可重新生成的 assistant 消息"                               |
| CHAT-REG-03  | 无对应 user 消息      | assistant 是首条   | POST regenerate                                              | 返回 400 "找不到对应的用户输入消息"                                       |
| CHAT-REG-04  | 重生成消息计数同步        | 重生成完成           | 查询 conversation                                              | `message_count` 减去删除条数后 +新生成条数                              |

#### 3.4.4 工具调用详情查询

| 用例 ID      | 用例名称                           | 前置条件                           | 操作步骤                                                               | 预期结果                                |
| ---------- | ------------------------------ | ------------------------------ | ------------------------------------------------------------------ | ----------------------------------- |
| CHAT-TC-01 | 查询消息工具调用                       | 存在含 tool\_calls 的 assistant 消息 | GET `/api/v1/conversations/{conv_id}/messages/{msg_id}/tool-calls` | 返回 tool\_calls 列表                   |
| CHAT-TC-02 | 兜底查 tool\_call/tool\_result 消息 | assistant 无 tool\_calls 字段     | 同上                                                                 | 查询其后 tool\_call/tool\_result 消息组装返回 |

***

### 3.5 记忆服务（mem-svc）

#### 3.5.1 短期记忆缓存

| 用例 ID     | 用例名称              | 前置条件          | 操作步骤                                                                | 预期结果                                          |
| --------- | ----------------- | ------------- | ------------------------------------------------------------------- | --------------------------------------------- |
| MEM-ST-01 | Redis 写入读取        | Redis 可用      | `set_messages` 后 `get_messages`                                     | 数据一致，TTL=86400s                               |
| MEM-ST-02 | 追加单条消息            | 已有缓存          | `append_message`                                                    | 列表末尾追加，count+1                                |
| MEM-ST-03 | 失效缓存              | 已有缓存          | `invalidate`                                                        | Redis key 删除，再次 get 返回空 list                  |
| MEM-ST-04 | Redis 不可用回退内存     | 关闭 Redis      | 任意操作                                                                | 触发 `_warn_fallback_once` 警告日志（仅一次），内存 dict 生效 |
| MEM-ST-05 | 内存缓存 TTL 过期       | 内存模式          | 等待 TTL 后 get                                                        | `_memory_expire` 清理，返回空                       |
| MEM-ST-06 | 非法数据类型防护          | Redis 存非 list | get\_messages                                                       | 返回空 list，记录 warning                           |
| MEM-ST-07 | clear\_cache 兼容接口 | 有缓存           | `clear_cache`                                                       | 返回 True，缓存清除                                  |
| MEM-ST-08 | 短期记忆查询（DB）        | 会话有消息         | GET `/api/v1/memory/agents/{agent_id}/short-term/{conversation_id}` | 按时间升序返回消息分页                                   |

#### 3.5.2 长期记忆

| 用例 ID     | 用例名称        | 前置条件        | 操作步骤                                             | 预期结果                                                                 |
| --------- | ----------- | ----------- | ------------------------------------------------ | -------------------------------------------------------------------- |
| MEM-LT-01 | 查询长期记忆（存在）  | Agent 有长期记忆 | GET `/api/v1/memory/agents/{agent_id}/long-term` | 返回 user\_profile/environment\_facts/experience/shared\_items/version |
| MEM-LT-02 | 查询长期记忆（不存在） | Agent 无记忆   | GET                                              | 返回空结构，version=0，不自动创建                                                |
| MEM-LT-03 | 更新长期记忆      | 存在          | PUT `/long-term`                                 | version+1，字段更新                                                       |
| MEM-LT-04 | 更新不存在兜底创建   | 不存在         | PUT                                              | 兜底创建，version=1                                                       |
| MEM-LT-05 | Agent 不存在   | 无           | GET 不存在 agent\_id                                | 返回 404 "Agent不存在"                                                    |
| MEM-LT-06 | 清空短期记忆      | 会话有消息       | DELETE 短期记忆                                      | 删除所有 messages，`conversation.message_count=0`，best-effort 清 Redis 缓存  |
| MEM-LT-07 | 清空缓存失败容错    | Redis 异常    | 清空短期记忆                                           | 缓存清除失败仅 warning，主流程不报错                                               |

#### 3.5.3 记忆摘要与深化

| 用例 ID       | 用例名称      | 前置条件    | 操作步骤                               | 预期结果                                                           |
| ----------- | --------- | ------- | ---------------------------------- | -------------------------------------------------------------- |
| MEM-SUM-01  | 记忆摘要（有记忆） | 存在长期记忆  | GET `/long-term/summary`           | 返回 `has_memory:true` + `preference_count/experience_count` 等统计 |
| MEM-SUM-02  | 记忆摘要（无记忆） | 不存在     | GET                                | 返回 `has_memory:false`，各 count=0                                |
| MEM-EVAL-01 | 记忆深化评估    | 对话历史存在  | 触发 `evaluate_and_update_long_term` | LLM 返回 JSON `{should_update, updates[]}`，按 segment 增删改长期记忆     |
| MEM-EVAL-02 | LLM 判断无更新 | 对话无价值信息 | 同上                                 | `should_update=false`，长期记忆不变                                   |
| MEM-EVAL-03 | 语义搜索（占位）  | 无       | `semantic_search_memory`           | 关键词匹配返回结果（Milvus 集成留空）                                         |

***

### 3.6 前端模块测试

| 用例 ID | 用例名称      | 模块                 | 操作步骤                             | 预期结果                                       |
| ----- | --------- | ------------------ | -------------------------------- | ------------------------------------------ |
| FE-01 | MCP 管理页   | views/mcp          | 列表/创建/连接/发现工具/启用禁用               | API 调用 `mcpApi`，状态实时刷新                     |
| FE-02 | Skill 管理页 | views/skill        | 列表/创建/导入本地/导入在线/渐进式加载            | Level 0/1/2 切换正常                           |
| FE-03 | Agent 绑定页 | views/agent        | 绑定/解绑 MCP+Skill，查看 tools-summary | 绑定列表实时更新                                   |
| FE-04 | 对话页 ReAct | views/chat         | 提问需工具的问题                         | SSE 流式渲染 + tool\_call 事件展示                 |
| FE-05 | 对话页停止/重生成 | views/chat         | 流式中点击停止 / 完成后重生成                 | `chatApi.stop` / `chatApi.regenerate` 正确调用 |
| FE-06 | 记忆页       | views/memory       | 查看长期记忆摘要 / 短期记忆                  | 数据正确展示                                     |
| FE-07 | API 拦截器   | api/index.js       | 触发非 200 响应                       | `message.error` 弹窗，Promise reject          |
| FE-08 | SSE 解析    | chatApi.sendStream | 收到 `data:[DONE]` 或 `done:true`   | 正确触发 onDone 回调                             |

***

## 4. 非功能测试

### 4.1 性能测试

| 用例 ID   | 场景                   | 指标    | 预期               |
| ------- | -------------------- | ----- | ---------------- |
| PERF-01 | MCP 列表查询             | 响应时间  | < 200ms（10 万行数据） |
| PERF-02 | 工具调用（SSE）            | 端到端延迟 | < 2s（不含工具自身耗时）   |
| PERF-03 | ReAct 多轮（5 轮）        | 总耗时   | < 30s            |
| PERF-04 | 短期记忆 Redis 读写        | 单次操作  | < 10ms           |
| PERF-05 | Skill 渐进式 Level 2 生成 | 响应时间  | < 100ms          |

### 4.2 异常与容错

| 用例 ID  | 场景            | 预期行为                                                   |
| ------ | ------------- | ------------------------------------------------------ |
| EXC-01 | MySQL 连接断开    | 接口返回 500，不崩溃                                           |
| EXC-02 | Redis 不可用     | 短期记忆回退内存，仅 warning 日志                                  |
| EXC-03 | LLM API 超时/限流 | LLMException，SSE 推送 error 事件并持久化 error 消息              |
| EXC-04 | tool-svc 不可用  | chat-svc 降级为纯文本对话                                      |
| EXC-05 | agent-svc 不可用 | tools-summary 返回 None，ReAct 关闭                         |
| EXC-06 | STDIO 子进程崩溃   | `is_connected` 返回 false，`_send_jsonrpc` 抛 MCPException |
| EXC-07 | httpx 未安装     | `_HTTPX_AVAILABLE=false`，工具调用返回降级结构                    |
| EXC-08 | 并发同一会话对话      | `_stop_events` 字典正确管理，无脏数据                             |

### 4.3 安全测试

| 用例 ID  | 场景                | 预期                                        |
| ------ | ----------------- | ----------------------------------------- |
| SEC-01 | LLM api\_key 加密存储 | DB 中为密文，运行时通过 `crypto_service.decrypt` 解密 |
| SEC-02 | .env 不入 Git       | `.gitignore` 已包含                          |
| SEC-03 | STDIO env 注入      | `exec_env` 仅合并不覆盖系统环境                     |
| SEC-04 | SQL 注入            | 全部使用 SQLAlchemy ORM 参数化查询                 |
| SEC-05 | XSS（前端）           | Vue 默认转义，无 v-html 滥用                      |

***

## 5. 测试数据准备

### 5.1 数据库初始化

```bash
mysql -u root -p < infra/mysql/init.sql
```

关键表（P2）：`mcp_services` / `mcp_tools` / `agent_mcp_bindings` / `skills` / `skill_levels` / `agent_skill_bindings` / `tool_call_logs`

### 5.2 测试用 MCP 服务

- **SSE 模式**: 启动任一兼容 MCP 协议的 HTTP 服务（提供 `/list_tools`、`/call_tool`）
- **STDIO 模式**: 准备一个读取 stdin、输出 stdout JSON-RPC 的可执行脚本

### 5.3 测试用 Skill 文件

- 准备一份含 frontmatter 的 Markdown Skill 文件（用于本地导入）
- 准备一份在线 URL（用于在线导入）

***

## 6. 缺陷分级与退出标准

### 6.1 缺陷分级

| 级别    | 定义         | 示例                           |
| ----- | ---------- | ---------------------------- |
| P0 致命 | 核心功能不可用    | ReAct 循环崩溃、MCP 无法连接、DB 初始化失败 |
| P1 严重 | 主流程受阻      | 工具调用失败无降级、流式对话中断、记忆丢失        |
| P2 一般 | 次要功能异常     | 渐进式 Level 2 截断异常、日志缺失字段      |
| P3 轻微 | UI/文案/性能优化 | 分页样式、提示文案、响应稍慢               |

### 6.2 退出标准

- P0 / P1 缺陷修复率 100%
- P2 缺陷修复率 ≥ 90%
- 核心用例（MCP-CALL-01、REACT-01、SK-L-02/03/04、MEM-ST-01、MEM-LT-03）全部通过
- 接口测试覆盖率 ≥ 80%
- 端到端 ReAct 流式对话手工验收通过

***

## 7. 附录

### 7.1 P2 新增 API 端点清单

| 服务        | 方法              | 路径                                                        |
| --------- | --------------- | --------------------------------------------------------- |
| tool-svc  | GET/POST        | `/api/v1/mcp-services`                                    |
| tool-svc  | GET/PUT/DELETE  | `/api/v1/mcp-services/{id}`                               |
| tool-svc  | POST            | `/api/v1/mcp-services/{id}/connect`                       |
| tool-svc  | POST            | `/api/v1/mcp-services/{id}/disconnect`                    |
| tool-svc  | POST            | `/api/v1/mcp-services/{id}/discover`                      |
| tool-svc  | GET             | `/api/v1/mcp-services/{id}/tools`                         |
| tool-svc  | POST            | `/api/v1/mcp-services/{id}/tools/{name}/toggle`           |
| tool-svc  | POST            | `/api/v1/tools/call`                                      |
| tool-svc  | GET             | `/api/v1/tool-call-logs`                                  |
| tool-svc  | GET/POST        | `/api/v1/skills`                                          |
| tool-svc  | GET/PUT/DELETE  | `/api/v1/skills/{id}`                                     |
| tool-svc  | POST            | `/api/v1/skills/{id}/toggle`                              |
| tool-svc  | GET             | `/api/v1/skills/{id}/levels`                              |
| tool-svc  | GET             | `/api/v1/skills/{id}/progressive`                         |
| tool-svc  | POST            | `/api/v1/skills/import/local`                             |
| tool-svc  | POST            | `/api/v1/skills/import/online`                            |
| agent-svc | GET/POST/DELETE | `/api/v1/agents/{id}/mcp-bindings`                        |
| agent-svc | GET/POST/DELETE | `/api/v1/agents/{id}/skill-bindings`                      |
| agent-svc | GET             | `/api/v1/agents/{id}/tools-summary`                       |
| chat-svc  | POST            | `/api/chat`（SSE）                                          |
| chat-svc  | POST            | `/api/chat/stop`                                          |
| chat-svc  | POST            | `/api/chat/regenerate`（SSE）                               |
| chat-svc  | GET             | `/api/v1/conversations/{id}/messages/{msg_id}/tool-calls` |
| mem-svc   | GET/PUT         | `/api/v1/memory/agents/{id}/long-term`                    |
| mem-svc   | GET             | `/api/v1/memory/agents/{id}/long-term/summary`            |
| mem-svc   | GET             | `/api/v1/memory/agents/{id}/short-term/{conv_id}`         |

### 7.2 关键代码位置索引

- MCP 适配器: [services/shared/domain/mcp\_adapter.py](file:///c:/Users/zhudaoren/Desktop/AgentService项目/agent-service-platform-phase1/services/shared/domain/mcp_adapter.py)
- Skill 管理器: [services/shared/domain/skill\_manager.py](file:///c:/Users/zhudaoren/Desktop/AgentService项目/agent-service-platform-phase1/services/shared/domain/skill_manager.py)
- MCP 业务服务: [services/tool-svc/services/mcp\_service.py](file:///c:/Users/zhudaoren/Desktop/AgentService项目/agent-service-platform-phase1/services/tool-svc/services/mcp_service.py)
- Skill 业务服务: [services/tool-svc/services/skill\_service.py](file:///c:/Users/zhudaoren/Desktop/AgentService项目/agent-service-platform-phase1/services/tool-svc/services/skill_service.py)
- ReAct 编排: [services/chat-svc/services/chat\_service.py](file:///c:/Users/zhudaoren/Desktop/AgentService项目/agent-service-platform-phase1/services/chat-svc/services/chat_service.py)
- 短期记忆缓存: [services/mem-svc/services/short\_term\_cache.py](file:///c:/Users/zhudaoren/Desktop/AgentService项目/agent-service-platform-phase1/services/mem-svc/services/short_term_cache.py)
- 记忆业务服务: [services/mem-svc/services/memory\_service.py](file:///c:/Users/zhudaoren/Desktop/AgentService项目/agent-service-platform-phase1/services/mem-svc/services/memory_service.py)
- DB Schema: [infra/mysql/init.sql](file:///c:/Users/zhudaoren/Desktop/AgentService项目/agent-service-platform-phase1/infra/mysql/init.sql)
- 前端 API: [frontend/src/api/index.js](file:///c:/Users/zhudaoren/Desktop/AgentService项目/agent-service-platform-phase1/frontend/src/api/index.js)

