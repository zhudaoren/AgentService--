# P2 阶段代码修改文档

> **版本**：Phase-2-v2.0.0
> **基线**：`810ecc1` (init: AgentService Platform Phase1, 2026-08-02)
> **终点**：`bd861bc` (feat(P2): 长期记忆自动评估 + 流式思考/回答分离 + 技能按需筛选, 2026-08-20)
> **统计**：108 文件变更，+31,958 / -1 行

---

## 一、P2 阶段开发时间线

| 提交 | 日期 | 主题 | 说明 |
|---|---|---|---|
| `6353665` | 2026-08-02 | release: Phase 2 (工具域P2开发完成) | P2 主体功能交付：MCP 双模式、Skill 管理、ReAct 循环、记忆深化 |
| `4fee1df` | 2026-08-03 | feat: P2 前端页面增强 | MCP 管理页、Skill 管理页、Agent 绑定面板、对话工具可视化 |
| `9ffcf7d` | 2026-08-06 | feat: 中间件一键启停脚本 | 跨平台 start/stop-middleware 脚本、Docker 镜像清单 |
| `082e4cb` | 2026-08-19 | feat(P2): 流式分离修复 + MCP fallback | SSE 事件分离、web_search 兜底、OAuth 服务、用户消息交互 |
| `bd861bc` | 2026-08-20 | feat(P2): 长期记忆评估 + 思考/回答分离 + 技能筛选 | 异步记忆评估、缓冲式事件路由、名称优先技能匹配 |

---

## 二、项目结构重组

### 2.1 目录层级扁平化

P1 阶段所有代码嵌套在 `agent-service-platform/` 子目录下，P2 阶段将其提升到仓库根目录，简化路径引用。

**P1 结构**：
```
agent-service-platform/
├── frontend/
├── infra/
├── scripts/
└── services/
```

**P2 结构**：
```
.
├── frontend/
├── infra/
├── scripts/
├── services/
├── requirements.txt          # 新增：统一 Python 依赖
├── _launch_backends.py       # 新增：一键启动所有后端
└── _selftest_chat.py         # 新增：聊天自测脚本
```

### 2.2 .gitignore 完善

新增临时调试脚本和 IDE 目录的忽略规则：
- `_test_skill_filter*.py` — 一次性技能筛选测试脚本
- `.trae/` — Trae IDE 临时目录
- `logs/` — 服务运行日志

---

## 三、后端代码修改

### 3.1 tool-svc 工具域（全新开发）

P1 阶段 tool-svc 仅有 `Dockerfile` + `main.py` 空壳。P2 完成完整工具域开发：

**路由层 (`routers/`)**：

| 文件 | 行数 | 职责 |
|---|---|---|
| `mcp.py` | 144 | MCP 服务 CRUD、连接管理、工具发现 |
| `skill.py` | 184 | Skill CRUD、本地/在线导入、渐进式披露 |
| `tool_call.py` | 90 | 工具调用代理、调用日志记录 |
| `oauth.py` | 309 | OAuth 认证流程（082e4cb 新增） |

**服务层 (`services/`)**：

| 文件 | 行数 | 核心能力 |
|---|---|---|
| `mcp_service.py` | 870 | MCP 双模式接入：SSE Adapter + STDIO Adapter (JSON-RPC 2.0)；连接池管理；工具发现与调用；web_search 内置 fallback（DuckDuckGo + 结构化 mock） |
| `skill_service.py` | 910 | Skill 管理：本地文件导入、在线 URL 导入、渐进式披露 Level 0/1/2；成功次数统计；绑定关系管理 |
| `oauth_service.py` | 976 | OAuth 2.0 完整流程：授权码、token 交换、刷新；支持 GitHub/Google 等多 provider |

### 3.2 shared/domain 共享域增强

**LLM 适配器 (`llm_adapter.py`, 487 行)**：
- 多 provider 支持：OpenAI / DeepSeek / Moonshot / Zhipu / SiliconFlow / GLM
- 流式调用 `astream()` + 非流式 `ainvoke()`
- **reasoning_content 捕获**：DeepSeek R1 等思考模型的 reasoning token 逐块累积，用于下一轮回传（避免 API 400 错误）
- 降级重试机制：参数失败后用默认参数重试一次
- 多模态消息支持：user content 数组降级（视觉模型不支持时）

**MCP 适配器 (`mcp_adapter.py`, 416 行)**：
- SSE 模式：HTTP+SSE 长连接，支持 JSON-RPC 2.0
- STDIO 模式：子进程 + stdin/stdout 管道
- 统一接口：`connect()` / `list_tools()` / `call_tool()` / `disconnect()`
- 连接池与心跳检测

**Skill 管理器 (`skill_manager.py`, 307 行)**：
- 渐进式披露三级：
  - Level 0：名称 + 一句话描述（注入 system prompt）
  - Level 1：详细描述 + 参数说明（按需加载）
  - Level 2：完整定义 + 示例（工具调用前加载）
- 按需加载：基于用户查询关键词筛选相关技能

**数据模型 (`models.py`, 263 行)**：
- 新增 `MCPService`：MCP 服务配置（类型、URL/命令、状态）
- 新增 `Skill`：Skill 定义（名称、描述、参数 schema、披露级别）
- 新增 `ToolCall` / `ToolCallLog`：工具调用记录与日志
- 新增 `AgentSkillBinding` / `AgentMCPBinding`：Agent 绑定关系

### 3.3 chat-svc 对话服务（大幅增强）

**核心服务 (`chat_service.py`, 2615 行)**：

P1 仅有基础流式对话，P2 扩展为完整的 ReAct 多轮推理引擎：

| 功能模块 | 说明 |
|---|---|
| **ReAct 主循环** | 最多 8 轮迭代；支持 function calling + 文本 ReAct 解析双模式；中间推理判定 `_looks_like_intermediate_thinking` |
| **流式事件分离** | 缓冲每轮 LLM content，流结束后按 `has_tool_calls` 判断：中间轮 → `thinking` 事件 → 思考区；最终轮 → `message` 事件 → 回答区 |
| **Plan-and-Execute** | 计划生成 → 分步执行 → 显式综合总结 LLM 调用 |
| **MCP 工具调用** | 流式累积 `tool_call_chunks`；自动补全 `mcp_service_name`；工具结果持久化 |
| **技能按需筛选** | `_select_relevant_skills`：名称直接命中优先，有名称命中时排除纯描述匹配；中文滑动窗口 + 英文驼峰拆分关键词提取 |
| **长期记忆触发** | `_trigger_memory_evaluation`：对话结束后异步调用 mem-svc `/evaluate` 接口；best-effort 模式，失败不影响主流程 |
| **Graceful degradation** | LLM 调用失败时，已有有效回答 + 错误提示；降级合成最终回答 |
| **reasoning_content 回传** | DeepSeek R1 思考模型的 reasoning token 累积并回传下一轮 |

**路由层 (`routers/chat.py`, 202 行)**：
- SSE 流式接口 `/chat/conversations/{id}/stream`
- 中断控制：`stop_event` 支持用户主动停止
- 消息持久化：user/assistant/tool_call/tool_result 全链路记录

**记忆服务 (`services/memory_service.py`, 300 行)**：
- 短期记忆：从 messages 表查询会话历史
- 长期记忆：对话历史 → LLM 分析 → 结构化更新

### 3.4 agent-svc 增强

| 文件 | 行数 | 增强内容 |
|---|---|---|
| `agent_service.py` | 1039 | Agent CRUD + MCP/Skill 绑定管理 + `tools-summary` 接口（聚合 MCP 工具 + Skill 列表） |
| `llm_service.py` | 239 | LLM 配置管理；API Key 加密存储（`crypto_service`） |
| `routers/agent.py` | 251 | Agent 路由 + 绑定关系 API |

### 3.5 mem-svc 记忆服务增强

**记忆业务服务 (`memory_service.py`, 985 行)**：

| 方法 | 职责 |
|---|---|
| `evaluate_and_update_long_term` | 对话历史 → LLM 分析 → 结构化 JSON → 更新 long_term_memory |
| `get_memory_summary` | 返回记忆摘要 + 实际内容字段（user_profile/environment_facts/experience） |
| `semantic_search_memory` | 语义搜索（Milvus 未集成，回退关键词匹配） |

**记忆评估 Prompt**（硬编码模板）：
- 输入：对话历史文本
- 输出：结构化 JSON `{should_update, updates:[{segment, action, key, value, confidence}]}`
- 三个 segment：`user_profile`（用户画像）、`environment_facts`（环境事实）、`experience`（经验教训）

**短期缓存 (`short_term_cache.py`, 247 行)**：
- Redis 短期记忆缓存（TTL 可配）
- `get_messages()` / `set_messages()` / `invalidate()`

### 3.6 gateway 网关

**`gateway/main.py` (480 行)**：
- 请求路由：按路径前缀分发到 5 个后端服务
- SSE 透传：流式响应直接透传，不缓冲
- 健康检查：`/healthz` 聚合各服务状态

---

## 四、前端代码修改

### 4.1 对话主界面 (`chat/index.vue`, 3605 行)

P1 仅有基础对话气泡，P2 增强为完整的对话交互中心：

| 功能模块 | 说明 |
|---|---|
| **SSE 事件处理** | `thinking_start` / `thinking` / `thinking_done` / `thinking_to_answer` / `message` / `plan_generated` / `skills_available` / `tool_call` / `done` 全事件链路 |
| **思考/回答分离** | 思考区显示中间推理（多轮累加 + 分隔标记）；回答区显示最终回答（message 事件流式追加） |
| **工具调用可视化** | 工具卡片：名称、参数、状态（pending/success/error）、结果摘要、耗时 |
| **Plan-and-Execute** | 计划卡片 checklist 样式 + 状态指示器（执行中/已完成/失败） |
| **技能展示** | 本次对话绑定的技能列表（名称、描述、类别） |
| **用户消息交互** | 复制/删除/回撤用户消息；复制 Agent 最终回答；重新生成按钮 |
| **异常隔离** | `onMessage`/`onDone`/`onError` 全局 try/catch，单事件异常不中断 SSE |
| **历史对话刷新** | 刷新页面后正确合并工具状态（修复"执行中"卡死） |
| **关键词高亮** | `indexOf` 字符串拼接替代动态正则（修复 SyntaxError） |

### 4.2 MCP 管理页 (`mcp/index.vue`, 1553 行，全新)

- SSE/STDIO 双模式创建表单
- 连接管理：连接/断开/状态检测
- 工具发现与开关控制
- 工具测试面板（参数输入 + 调用 + 结果展示）

### 4.3 Skill 管理页 (`skill/index.vue`, 1055 行，全新)

- Skill CRUD 操作
- 本地文件导入（zip 包解析）
- 在线 URL 导入
- 渐进式披露三级查看（Level 0/1/2 切换）
- 成功次数统计展示

### 4.4 Agent 管理页 (`agent/index.vue`, 1422 行，增强)

- MCP/Skill 绑定折叠面板
- 工具汇总 Drawer（聚合 MCP 工具 + Skill 列表）
- Agent 配置编辑（LLM 配置、温度、max_tokens）

### 4.5 记忆管理页 (`memory/index.vue`, 525 行)

- 长期记忆编辑（user_profile / environment_facts / experience）
- 短期记忆查看（按会话查看消息历史）
- 记忆摘要 Modal（展示实际记忆内容字段）

### 4.6 新增组件

| 组件 | 行数 | 说明 |
|---|---|---|
| `MermaidBlock.vue` | 137 | Mermaid 图表渲染组件（用于对话中的流程图/时序图） |

### 4.7 API 层 (`api/index.js`, 452 行)

新增 API 封装：
- `mcpApi` — MCP 服务管理
- `skillApi` — Skill 管理
- `memoryApi` — 记忆管理（长期/短期/摘要）
- `toolCallApi` — 工具调用

---

## 五、数据库变更

### 5.1 新增表

| 表名 | 说明 |
|---|---|
| `mcp_services` | MCP 服务配置（name, type, url/command, status, tools_json） |
| `skills` | Skill 定义（name, description, parameters, disclosure_level） |
| `tool_calls` | 工具调用记录（tool_name, arguments, result, status, duration_ms） |
| `agent_mcp_bindings` | Agent-MCP 绑定关系 |
| `agent_skill_bindings` | Agent-Skill 绑定关系 |
| `long_term_memories` | 长期记忆（user_profile, environment_facts, experience, version） |

### 5.2 迁移脚本

| 文件 | 说明 |
|---|---|
| `20260814_add_thinking_to_messages.sql` | messages 表增加 thinking 字段（中间推理持久化） |
| `20260819_add_attachments_to_messages.sql` | messages 表增加 attachments 字段（多模态附件） |
| `20260819_add_builtin_flag.sql` | skills 表增加 is_builtin 标记 |
| `20260819_add_skill_success_count.sql` | skills 表增加 success_count 统计字段 |
| `20260819_rename_drawing_agent_to_design_mermaid.sql` | 内置 Agent 重命名（drawing → design_mermaid） |

### 5.3 初始化脚本

`infra/mysql/init.sql` (472 行) 完整重写：
- 8 个微服务的表结构定义
- 索引优化
- 内置数据（官方 Agent、默认 LLM 配置）

---

## 六、基础设施与脚本

### 6.1 Docker 环境

**docker-compose.yml** (399 行)：
- 新增服务：Redis、MinIO、Milvus（向量库）
- 8 个微服务容器定义
- 网络与数据卷配置

### 6.2 启动/停止脚本

| 脚本 | 说明 |
|---|---|
| `scripts/start-middleware.py/.sh` | 中间件一键启动（Redis/MinIO/Milvus），自动拉取镜像、端口检测、健康检查等待 |
| `scripts/stop-middleware.py/.sh` | 中间件一键停止 |
| `scripts/run_svc_bg.py` | 后台启动单个微服务（uvicorn + 自动重启） |
| `scripts/run_svc.bat` | Windows 后台启动封装 |
| `_launch_backends.py` | 一键启动所有 5 个后端服务（gateway/agent/chat/tool/mem） |
| `scripts/seed_via_mysql.py` | 数据库种子数据（官方 Agent、LLM 配置、示例 MCP/Skill） |
| `scripts/seed_initial_data.py/.ps1` | 跨平台种子数据脚本 |
| `_selftest_chat.py` | 聊天自测脚本（SSE 流式验证 + 持久化检查） |
| `scripts/run_sse_integration_tests.py` | SSE 集成测试 |

---

## 七、关键技术决策

### 7.1 流式思考/回答分离方案

**问题**：ReAct 多轮推理中，最终回答内容会先出现在思考区块，再迁移到回答区块，造成视觉错位。

**方案演进**：

1. **方案 A（082e4cb）**：`thinking_to_answer` 事件 + 前端迁移
   - 后端流式期间用 `thinking` 事件推送所有内容
   - 流结束后发 `thinking_to_answer` 信号
   - 前端把 thinking 区内容迁移到回答区
   - **缺陷**：内容先在思考区显示，再迁移，仍有闪烁

2. **方案 B（bd861bc，最终方案）**：后端缓冲 + 事件路由
   - 后端流式期间**缓冲所有 content**，不立即 yield
   - 流结束后根据 `has_tool_calls` 判断：
     - 有 tool_calls（中间轮）→ `thinking` 事件 → 思考区
     - 无 tool_calls（最终轮）→ `message` 事件 → 回答区
   - 前端 `thinking_to_answer` 仅保留中间推理或清空，不做迁移
   - **代价**：失去最终轮的流式打字效果，但彻底消除错位

### 7.2 长期记忆评估链路

**问题**：多轮对话后长期记忆和摘要为空。

**方案**：
- chat-svc 对话结束后异步调用 mem-svc `/evaluate` 接口
- mem-svc 加载 Agent 的 LLM 配置，动态创建适配器（复用 Agent 绑定的模型）
- 结构化 Prompt 要求 LLM 返回 JSON：`{should_update, updates:[{segment, action, key, value}]}`
- 按 segment（user_profile/environment_facts/experience）分别应用更新
- version+1 持久化

### 7.3 技能按需筛选

**问题**：所有技能全量加载，上下文开销大且不精准。

**方案**：
- 关键词提取：中文 2/3/4 字滑动窗口 + 英文驼峰/分隔符拆分
- 两级匹配：名称直接命中（强相关）优先；描述命中（弱相关）次选
- 有名称命中时**只返回名称命中的技能**，排除纯描述匹配
- 无任何命中 → 回退全量（保守策略）

### 7.4 MCP 工具 fallback

**问题**：MCP 服务不可用时工具调用失败，影响对话体验。

**方案**：
- `web_search` 内置 fallback：MCP 服务不可用时走 DuckDuckGo + 结构化 mock
- 工具调用状态记录：`pending` → `success` / `error`
- 调用日志持久化到 `tool_calls` 表

---

## 八、P2 阶段交付清单

### 后端

- [x] tool-svc 完整工具域（MCP + Skill + OAuth + tool_call）
- [x] chat-svc ReAct 多轮推理引擎（8 轮迭代 + function calling + 文本 ReAct）
- [x] 流式思考/回答分离（缓冲 + 事件路由）
- [x] 长期记忆自动评估链路（chat-svc → mem-svc → LLM → 持久化）
- [x] 技能按需筛选（名称优先匹配）
- [x] MCP 工具 fallback（web_search 兜底）
- [x] Plan-and-Execute 工作模式
- [x] reasoning_content 回传（DeepSeek R1 兼容）
- [x] Graceful degradation（LLM 失败降级）

### 前端

- [x] 对话主界面（SSE 全事件链路 + 工具可视化 + Plan&Execute）
- [x] MCP 管理页（双模式创建 + 连接管理 + 工具测试）
- [x] Skill 管理页（CRUD + 导入 + 渐进式披露）
- [x] Agent 管理页增强（绑定面板 + 工具汇总 Drawer）
- [x] 记忆管理页（长期/短期/摘要）
- [x] MermaidBlock 组件

### 基础设施

- [x] Docker Compose（Redis/MinIO/Milvus）
- [x] 中间件一键启停脚本
- [x] 后端一键启动脚本
- [x] 数据库种子脚本
- [x] 自测脚本
- [x] 5 个数据库迁移脚本

---

## 九、P3 阶段待办

- [ ] Milvus 向量库集成（语义搜索记忆）
- [ ] RAG 服务完善（rag-svc 目前空壳）
- [ ] ChatBI 服务完善（chatbi-svc 目前空壳）
- [ ] 协作服务完善（coord-svc 目前空壳）
- [ ] 进化服务完善（evo-svc 目前空壳）
- [ ] 综合摘要 LLM 生成（summary 字段目前为空字符串预留）
- [ ] OAuth 完整集成测试
